services:
  - type: web
    name: smollm-mini
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:8080
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
